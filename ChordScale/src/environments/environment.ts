// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyA12RwF14jLUNvuCtILimHYTAVSN4yALjE",
    authDomain: "chord-scale.firebaseapp.com",
    databaseURL: "https://chord-scale-default-rtdb.firebaseio.com",
    projectId: "chord-scale",
    storageBucket: "chord-scale.appspot.com",
    messagingSenderId: "228961422061",
    appId: "1:228961422061:web:2a421cf76cbc3a235f1ae4"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
